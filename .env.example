# MilkDrop Planner Skill — environment variables
# Copy this file to .env and adjust as needed.
# The skill server itself requires no API keys — it is fully stateless.
# API keys belong in your AI agent's configuration, not here.

# Port the skill server listens on (default: 3000)
PORT=3000
